; Terraform shares HCL's node types, so the same rules apply. Upstream said
; `inherits: hcl`; Lumine has no inheritance, so they are written out.
[
  (comment)
  (block)
  (heredoc_template)
  (object)
] @fold
